# ATM

Small ATM example using a binary file to store client balances.

Build:

```
make build
```

Run:

```
make run
```

Notes: Includes `atm.c`, `file.c`, `file.h`, `profile.h`. The `Makefile` builds the executable.
