# Binary Search

Simple iterative binary-search implementation in `binarysearch.c`. The implementation returns the index of the found element or `-1` if not found.

Build:

```
make
```

Run: see the `Makefile` (the executable is built in the project folder).
