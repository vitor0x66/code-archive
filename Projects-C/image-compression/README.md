# Image Compression

Simple P3 PPM image processing example that maps pixels to a small palette and prints counts per row.

Build:

```
make
```

Run with P3 PPM input on stdin, for example:

```
./image-compression < input.ppm
```
