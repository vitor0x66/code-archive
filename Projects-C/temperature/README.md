# Temperature

Temperature conversion utility. Conversion functions were corrected (fixed integer-division and incorrect recursion). The program converts between Celsius, Fahrenheit and Kelvin.

Build and run:

```
make
make run
```
