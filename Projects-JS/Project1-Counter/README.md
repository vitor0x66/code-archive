# Project1-Contador

Simple counter demo implemented in JavaScript. Self-contained; no external dependencies.

Include: Yes — small, useful as a course example.

Run: open the `index.html` (if present) or inspect `index.js` in a browser.
