# Project11-WeatherApp

Simple weather application demo. Likely depends on an external API (e.g., OpenWeather) and may require an API key.

Include: Yes — keep for reference, but note that API keys or environment setup may be required.

Run: inspect `index.js` for API usage and follow any instructions to provide an API key.
